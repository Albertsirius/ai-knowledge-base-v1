帮我编写一个 Python 脚本 hooks/validate_json.py 用于校验知识条目 JSON 文件：

需求：
1. 支持单文件和多文件 （通配符 *.json）两种输入模式
2. 检查 JSON 是否正确解析
3. 必填字段使用 dict[str, type] 格式，同时校验字段存在性和类型：id(str), title(str), url(str), summary(str), tags(list), status(str)
4. 检查 ID 格式是否为 {source}-{slug}-YYYY-MM-DD-NNN（例如：github-trending-2026-06-19-001）
5. 检查摘要最少20字，标签至少1个
6. 检查 score （如有）是否在 1-10 范围
7. 命令行用法: python hooks/validate_json.py <json_file> [json_file2...] 或者使用通配符: python hooks/validate_json.py *.json
8. 校验通过 exit 0，否则 exit 1 + 错误列表 +汇总统计

编码规范：遵循 PEP 8，使用 pathlib，不依赖第三方库
